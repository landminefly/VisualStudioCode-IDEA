<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapper.UserMapper">
    <select id="getAllUser" resultType="user">
        select * from user;
    </select>

    <select id="getAllUserInMapUsingList" resultType="map">
        select * from user;
    </select>

    <select id="getAllUserInMapUsingMap" resultType="map">
        select * from user;
    </select>
</mapper>